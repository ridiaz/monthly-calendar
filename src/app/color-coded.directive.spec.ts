import { ColorCodedDirective } from './color-coded.directive';

describe('ColorCodedDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCodedDirective();
    expect(directive).toBeTruthy();
  });
});
